{
  "name": "@sr3h/ai-presence-checker",
  "version": "0.10.5",
  "private": true,
  "type": "module",
  "description": "Read-only AIDO website access, understanding and AI discoverability checks.",
  "scripts": {
    "generate:skill": "node scripts/generate-skill-bundle.mjs",
    "precheck": "npm run generate:skill",
    "check": "node --check src/index.js && node --check src/aido-report-ui.js && node --check src/audit.js && node --check src/discoverability.js && node --check src/extended-research.js && node --check src/usage-guard.js && node --check src/url-safety.js",
    "pretest": "npm run generate:skill",
    "test": "node --test",
    "probe:remote": "node scripts/probe-remote.mjs",
    "verify:remote": "node scripts/verify-remote.mjs",
    "verify:extended:remote": "node scripts/verify-extended-remote.mjs",
    "verify:skill:remote": "node scripts/verify-skill-import.mjs",
    "verify:web:remote": "node scripts/verify-web-check.mjs",
    "dev": "wrangler dev",
    "predeploy": "npm run check && npm test",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
